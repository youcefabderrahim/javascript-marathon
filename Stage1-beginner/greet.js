// This is a function that greets someone's by name
function greet (name) {
    // ! Use backti
    console.log(`hello ${name}`);
}

let name = 'alice';
greet(name);



document.getElementById(`paragraph`).textContent = `Hello ${name}`